# FactoryMethodPatternCSharp

C# example of the Factory Method pattern.

Build and run:

```powershell
cd "Exercise_1-Singleton\FactoryMethodPatternCSharp"
dotnet run --project "FactoryMethodPatternCSharp.csproj"
```
