# Lab 6: Updating and Deleting Records

## Scenario

The store updates product prices and removes discontinued items.

## Implemented

- Updates `Laptop` price to `70000`.
- Deletes `Rice Bag`.
- Saves each change with `SaveChangesAsync`.

## Commands

```powershell
dotnet run --project .\Lab06-Update-Delete.csproj
```
